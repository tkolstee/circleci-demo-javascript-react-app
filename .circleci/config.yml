---
version: 2.1
workflows:
  plugh:
    jobs:
      - test
jobs:
  test:
    docker:
      # Found on CircleCI website
      - image: cimg/node:22.4.1
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: run tests
          command: npm run test

